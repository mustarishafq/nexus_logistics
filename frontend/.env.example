# Optional: override API URL (defaults to /api via Vite proxy in dev)
# VITE_API_URL=http://localhost:8000/api

# Production build: API URL for the React app (use /api when the frontend host proxies /api to Laravel)
# VITE_API_URL=/api
# VITE_API_PROXY_TARGET=https://logisticsapi.emzinexus.com
