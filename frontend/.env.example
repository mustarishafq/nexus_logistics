# Dev: defaults to /api (proxied to Laravel by Vite). Override only if needed:
# VITE_API_URL=http://localhost:8000/api

# Production: keep /api so users only see the frontend domain.
# VITE_API_URL=/api
# VITE_API_PROXY_TARGET=https://logisticsapi.emzinexus.com
