# Nexus Logistics Frontend

React SPA for the Nexus Logistics platform. See the [root README](../README.md) for setup and run instructions.

```bash
npm run dev
```

The dev server proxies `/api` requests to the Laravel backend at `http://localhost:8000`.
